# CLAUDE CODE — ТОЧКА ВХОДА

> Читается автоматически при старте каждой сессии.

## Контекст проекта (загружается автоматически)
@brain/infra.md
@brain/grabli.md
@brain/projects.md
@brain/rules.md

## Старт сессии
1. `git pull`
2. Если работаем над конкретным проектом — прочитай его `context.md`
3. Напиши одной строкой: «Прочитано: brain/ + [проект если был]»

## Конец сессии
По команде «пуш» или «конец сессии»:
1. Обнови нужные файлы в `brain/` и папке проекта
2. `git add . && git commit -m "описание" && git push`

## Правила
- Пути всегда относительные
- Грабли только дополнять, никогда не удалять
- После каждой задачи — коммит
- Язык общения: русский
